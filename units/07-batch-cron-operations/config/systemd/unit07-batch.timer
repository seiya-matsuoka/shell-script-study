# Unit 07 learning example.
# service と timer を分け、timer が service の起動 schedule を担当する。

[Unit]
Description=Run Unit 07 sample batch job periodically

[Timer]
OnCalendar=*:0/5
Persistent=true
Unit=unit07-batch.service

[Install]
WantedBy=timers.target
